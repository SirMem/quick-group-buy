<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IAuthTokenDao">

    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.AuthRefreshToken">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="token_hash" property="tokenHash"/>
        <result column="status" property="status"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.AuthRefreshToken">
        insert into auth_refresh_token(openid, token_hash, status, expire_time, create_time, update_time)
        values(#{openid}, #{tokenHash}, #{status}, #{expireTime}, now(), now())
    </insert>

    <select id="queryByTokenHash" parameterType="java.lang.String" resultMap="dataMap">
        select id, openid, token_hash, status, expire_time, create_time, update_time
        from auth_refresh_token
        where token_hash = #{tokenHash}
    </select>

    <update id="updateStatus">
        update auth_refresh_token
        set status = #{newStatus}, update_time = now()
        where token_hash = #{tokenHash} and status = #{oldStatus}
    </update>

</mapper>
